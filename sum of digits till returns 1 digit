#include<stdio.h>
int sum(int n);
int main()
{
	int x,r,t;
	scanf("%d",&x);
	r=sum(x);
	t=sum(r);
	printf("%d",t);
	return 0;
}
int sum(int x)
{
	if(x!=0)
	{
		return (x%10+sum(x/10));
	}
	
	else
	{
		return 0;
	
}
}
